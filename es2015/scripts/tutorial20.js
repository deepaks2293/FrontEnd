let colors = ['Orange', 'White', 'DarkBlue', 'Green'];
//Normal Java
for (let index in colors) {
    console.log(colors[index]);
}
for (let color of colors) {
    console.log(color);
}
let letter = "ABC";
for (let letters of letter) {
    console.log(letters);
}
