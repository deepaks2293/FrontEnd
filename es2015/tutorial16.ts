let ln = "last name";
let person = {
    // Object literals can have spaces too
    "first name": "Deepak",
    //We can have variable name too assigned to object only need to be in brackets
    [ln]: "Singh"
}

console.log(person);